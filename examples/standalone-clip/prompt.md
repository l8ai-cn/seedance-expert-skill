# Standalone Prompt

Typed segments:

1. `binding(watch)`
2. ` is the watch reference; preserve the logo, case geometry, dial markings, and strap exactly. A narrow warm strip light sweeps once across the glass, revealing the face and brushed metal edge. Camera stays locked in a clean macro product angle. Sound: quiet room tone with one soft tick at the final highlight.`

`binding(watch)` is internal plan notation, not prompt text. Render it through a selected current surface profile.
